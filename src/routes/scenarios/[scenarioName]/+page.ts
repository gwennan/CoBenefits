import { base } from "$app/paths";

import Papa from "papaparse";

export async function load({ fetch, params }) {

     // let data = await fetch(`${base}/simulations_new/BNZ.csv`)
     //    .then(data => data.text())
     //    .then(data => {
     //        console.log("text")
     //        return data
     //    })

    // return data
    return {a: 20}
}